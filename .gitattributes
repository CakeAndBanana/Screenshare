# repo.json is rewritten by CI on every release and served raw to Dalamud clients - pin LF so a
# Windows checkout can't reintroduce CRLF and turn each release into a whole-file diff.
* text=auto eol=lf
